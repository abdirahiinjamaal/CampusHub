import { Check, Clock3 } from 'lucide-react';
export default function FeesCard({ fees }) { const percent = Math.round((fees.paid / fees.total) * 100); return <div className="fees-panel"><div className="fee-overview"><div><span>Total fees</span><strong>${fees.total.toLocaleString()}</strong></div><div><span>Amount paid</span><strong>${fees.paid.toLocaleString()}</strong></div><div className="outstanding"><span>Outstanding</span><strong>${fees.outstanding.toLocaleString()}</strong></div></div><div className="progress-row"><div className="progress-track"><div style={{ width: `${percent}%` }} /></div><span>{percent}% paid</span></div><div className="table-wrap"><table><thead><tr><th>Date</th><th>Description</th><th>Amount</th><th>Status</th></tr></thead><tbody>{fees.history.map((item) => <tr key={item.date + item.description}><td>{item.date}</td><td>{item.description}</td><td>${item.amount.toLocaleString()}</td><td><span className={`status ${item.status.toLowerCase()}`}>{item.status === 'Paid' ? <Check size={14} /> : <Clock3 size={14} />}{item.status}</span></td></tr>)}</tbody></table></div></div>; }
