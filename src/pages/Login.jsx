import { Eye, EyeOff, LockKeyhole, ArrowRight, ShieldCheck } from 'lucide-react';
import { useState } from 'react';
import { api } from '../services/api';
import BrandMark from '../components/BrandMark';

export default function Login({ onLogin }) { const [studentId, setStudentId] = useState('STU-2026-001'); const [password, setPassword] = useState('student123'); const [show, setShow] = useState(false); const [error, setError] = useState(''); const [loading, setLoading] = useState(false);
  async function submit(e) { e.preventDefault(); setError(''); setLoading(true); try { const result = await api.login(studentId.trim().toUpperCase(), password); if (!result) return setError('We couldn’t verify those details. Check your Student ID and password.'); onLogin(result); } catch { setError('Unable to reach the portal. Please try again later.'); } finally { setLoading(false); } }
  return <main className="login-page"><div className="login-shell"><section className="login-art"><div className="art-top"><BrandMark /><span className="secure-label"><ShieldCheck size={15} /> Secure student access</span></div><div className="art-copy"><p className="eyebrow">YOUR ACADEMIC HOME</p><h1>Everything you need<br /><em>to move forward.</em></h1><p>Check your progress, keep track of your fees, and stay close to your academic journey.</p></div><div className="art-footer"><span>Horizon University</span><span>Since 1987 · Nairobi</span></div></section><section className="login-form-wrap"><div className="login-form"><div className="mobile-brand"><BrandMark /></div><p className="eyebrow">STUDENT PORTAL</p><h2>Welcome back<span>.</span></h2><p className="form-intro">Sign in to access your academic information.</p><form onSubmit={submit}><label>Student ID<input value={studentId} onChange={e => setStudentId(e.target.value)} placeholder="STU-2026-001" autoComplete="username" required /></label><label>Password<div className="password-field"><input type={show ? 'text' : 'password'} value={password} onChange={e => setPassword(e.target.value)} placeholder="Enter your password" autoComplete="current-password" required /><button type="button" onClick={() => setShow(!show)} aria-label={show ? 'Hide password' : 'Show password'}>{show ? <EyeOff size={18} /> : <Eye size={18} />}</button></div></label>{error && <div className="form-error">{error}</div>}<button className="primary-btn" disabled={loading}>{loading ? 'Signing in…' : <>Sign in <ArrowRight size={18} /></>}</button></form><div className="login-note"><LockKeyhole size={15} /><span>Use <strong>STU-2026-001</strong> and <strong>student123</strong> to preview the portal.</span></div></div></section></div></main>; }
